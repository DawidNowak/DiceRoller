<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:DiceRoller.Controls;assembly=DiceRoller"
             x:Class="DiceRoller.Views.DiceCreatorPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout VerticalOptions="StartAndExpand">
                <Label Text="{Binding Path}" 
                       HorizontalOptions="CenterAndExpand" />
                <Button Text="Set Mini Image" Command="{Binding SetMiniImageCommand}"></Button>
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                    <Label Text="Mini Image"></Label>
                    <Image Source="{Binding MiniImageSource}" 
                           HeightRequest="36"
                           WidthRequest="36"/>
                </StackLayout>
                <Button Text="Add dice wall" Command="{Binding AddDiceWallCommand}"/>
                <controls:WrapLayout x:Name="DiceWallLayout"/>
            </StackLayout>
            <Button Text="Save" Command="{Binding SaveCommand}"></Button>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>